# 한계와 향후 계획

## 현재 한계

- 전국 초정밀 버스 API는 도착예정시간 필드를 제공하지 않는다.
- `PUBLIC_DATA_API_KEY`가 없으면 실데이터 대신 폴백 상태로 동작한다.
- 아이돌봄 기관 API 응답의 실제 데이터 품질에 따라 `stdgCd`가 비어 있을 수 있다.

## 향후 계획

- 실데이터 키 연결 후 기관별 이동 판단 정확도 검증
- 기관 데이터 정제와 `stdgCd` 정합성 보강
- 심사용 시나리오에 맞춘 데모 캡처와 제출 자료 시각화 강화
